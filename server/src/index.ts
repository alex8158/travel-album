// P0.T2 占位：仅完成 Node + TypeScript 工程的脚手架与构建验证。
// 后续任务会逐步引入：
//   P0.T4 集中配置层与 .env.example
//   P0.T5 SQLite 连接与迁移
//   P0.T6 结构化日志与统一错误响应
//   P0.T7 StorageProvider 抽象
//   P0.T8 ffmpeg / ffprobe 启动检测与 /api/health
//   P1.T3 Trip 路由
// 详见 docs/tasks.md。

const main = (): void => {
  console.log("travel-album server: scaffolded (P0.T2). No routes yet.");
};

main();
